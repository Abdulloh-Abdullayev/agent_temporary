

import 'package:flutter/material.dart';

import '../../widgets/appbar_main.dart';

class InterestingPage extends StatelessWidget {
  const InterestingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child:  Column(
        children: [

        ],
      ),
    );
  }
}
